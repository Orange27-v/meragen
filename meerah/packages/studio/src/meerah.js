"use client";

/**
 * Meerah's API surface for the studio components.
 *
 * Re-exported from the forked client so components can keep importing the same
 * function names. See the header of `muapi.js` for what changed and why.
 */
export * from './muapi.js';
